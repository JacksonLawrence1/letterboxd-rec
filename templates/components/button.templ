package components

templ Button(attrs templ.Attributes, class string) {
	<button class={ "inline-flex justify-center rounded-md text-white h-10 px-4 py-2 bg-primary-600 hover:bg-primary-700", class } { attrs... }>
		{ children... }
	</button>
}
